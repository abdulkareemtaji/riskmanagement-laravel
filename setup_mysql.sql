-- Create the risk management database
CREATE DATABASE IF NOT EXISTS risk_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Grant privileges (adjust as needed for your MySQL setup)
-- GRANT ALL PRIVILEGES ON risk_management.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;

USE risk_management;